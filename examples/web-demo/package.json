{
  "name": "@traceability/example-web-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "preview:build": "vite build --mode preview",
    "sourcemaps:upload": "node ./scripts/upload-sourcemaps.mjs",
    "preview:prepare": "pnpm preview:build && pnpm sourcemaps:upload",
    "preview": "vite preview --host 0.0.0.0 --port 4174"
  },
  "dependencies": {
    "@traceability/core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
